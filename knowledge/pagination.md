# 分页接口测试策略

## 分页参数
- page=0 → 视接口定义（有些从0开始，有些从1开始）
- page=1, size=10 → 正常返回
- page=最后一页 → 返回剩余数据
- page=超大值 → 返回空列表，不报错
- size=0 → 视接口定义
- size=-1 → 应返回 400
- size=超大值 → 应有上限保护

## 响应验证
- total 数量是否正确
- items 数量 <= size
- 翻页后数据不重复
